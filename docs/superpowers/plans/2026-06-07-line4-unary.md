# Line-4 unary (CLR/NEG/NEGX/NOT/TST/SWAP/EXT/TAS, register forms) Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development / executing-plans. Checkbox steps, TDD. Gate = LOCK-STEP + OOC sanity (FMax-neutral; post-route is non-deterministic — not gated). STANDING RULE: keep plugin boundaries registered.

**Goal:** CLR/NEG/NEGX/NOT/TST/SWAP/EXT(.W/.L)/EXTB.L/TAS on data registers. Lock-stepped vs Musashi.

**Architecture:** Single-operand ops on Dn. CLR/NEG/NEGX/NOT/TST reuse the ALU (0-Dn / ~Dn / test); SWAP/EXT are bit manipulations; TAS = test + set-bit7. `.B/.W` writes reuse the MOVE.B/.W partial-register merge (read old Dn — for NEG/NOT/NEGX the operand IS the source). Memory forms DEFERRED to mem-RMW.

**Tech Stack:** SpinalHDL / sbt `~/sbt/bin/sbt` / Verilator / Vivado(OOC sanity). Reference: `decode/OperationDecoder.scala`+`MicroOpAssembler.scala` (line 4 — CHK/TRAP/RTE already there; add the unary group), `execute/AluDatapath.scala`+`AluEuPlugin.scala` (NEG/NOT/NEGX + the .B/.W partial merge; add SWAP/EXT/CLR/TAS), `frontend/PredecodeWord.scala` (line-4 unary len1), `oracle/Musashi.scala`, `lockstep/ExecuteLockStepSpec.scala`. Spec: `docs/superpowers/specs/2026-06-07-line4-unary-design.md`. NOTE: NEGX/extended-arith Z-clear-only rule may already be modelled (SUBX/ADDX) — reuse.

**Branch:** `feat/line4-unary` (off master).

---

### Task 1: Diagnose — ALU reuse for NEG/NOT/NEGX, SWAP/EXT/CLR/TAS path, NEGX Z-rule, predecode, EXT vs SWAP opword decode
**Files:** read-only. Confirm: NEG=0-Dn / NEGX=0-Dn-X / NOT=~Dn via the ALU; whether NEGX's Z-clear-only rule is already modelled; the SWAP/EXT/EXTB/CLR/TAS implementation (ALU op or a small unary datapath); the line-4 unary opword decode (0x40/42/44/46/4A xx for NEGX/CLR/NEG/NOT/TST; 0x4840 SWAP; 0x4880/48C0/49C0 EXT/EXTB; 0x4AC0 TAS) — careful disambiguation from CHK (bit8=1) + the 0x4800-group; predecode len1.
- [ ] Capture decisions in the Task-1 commit (empty ok).

### Task 2: CLR/NEG/NEGX/NOT/TST (ALU-class)
**Files:** `decode/*`, `execute/AluDatapath.scala`+`AluEuPlugin.scala`, Test `decode/Line4DecodeSpec.scala` + lock-step.
Decode + implement CLR (Dn:=0, Z=1/N=0/V=0/C=0), NEG (0-Dn NZVCX), NEGX (0-Dn-X, Z-clear-only, NZVCX), NOT (~Dn, NZ), TST (flags only, no write). .B/.W partial merge.
- [ ] failing test (each, .B/.W/.L, flag edges incl. NEGX X + Z-preserve) → FAIL → implement → PASS ×2 → commit `line4: CLR/NEG/NEGX/NOT/TST (data-reg)`.

### Task 3: SWAP / EXT / EXTB.L / TAS
**Files:** `decode/*`, `execute/*`, Test extend.
SWAP (swap halves, full-32, NZ). EXT.W (byte→word .W), EXT.L (word→long), EXTB.L (byte→long), NZ. TAS (N/Z from Dn[7:0]; Dn[7]:=1, byte merge).
- [ ] failing test (SWAP, EXT.W/.L, EXTB.L, TAS Dn — value + flags) → FAIL → implement → PASS ×2 → commit `line4: SWAP/EXT/EXTB/TAS (data-reg)`.

### Task 4: Lock-step + OOC sanity
**Files:** `lockstep/ExecuteLockStepSpec.scala`, run suites.
- [ ] Step 1: lock-step vs Musashi ×2 — CLR/NEG/NEGX/NOT/TST .B/.W/.L (flag edges + NEGX X/Z-preserve), SWAP, EXT.W/.L, EXTB.L, TAS — value + NZVCX step-for-step. ALL existing UNCHANGED (ITLB flake → baseline-repro first).
- [ ] Step 2: `make test-fast` + targeted verilator `-z` subsets — report totals.
- [ ] Step 3: OOC sanity (`GenFullCoreSynthVerilog` + `ooc_M68kFullCoreSynth.tcl`): 0 err, no UNASSIGNED REGISTER. No new cross-module crossing.
- [ ] Step 4: commit `line4-unary: lock-step + OOC sanity`.

---

## Self-Review
**Spec coverage:** CLR/NEG/NEGX/NOT/TST (T2); SWAP/EXT/EXTB/TAS (T3); lock-step+sanity (T4). ✓
**Placeholder scan:** NEGX-Z-rule + SWAP/EXT/CLR/TAS path + opword decode are explicit T1 decisions (spec §5). No TBD.
**Type consistency:** NEG/NEGX/NOT/TST reuse ALU + the .B/.W partial merge (operand=source); CLR=zero write; SWAP/EXT=bit manip; TAS=test+set-bit7 byte merge; NZVCX via existing flag paths (NEGX reads/writes X). ✓
