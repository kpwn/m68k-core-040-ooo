# LINK / UNLK — implementation plan

Branch `feat/link-unlk` (off c898892). Spec: specs/2026-06-10-link-unlk-design.md.

## Task 1 — predecode framing (PredecodeWord line-4)
LINK (op[15:4]==0x4E5, op[3]==0): simple, lenWords=2 (opword + disp16).
UNLK (op[15:4]==0x4E5, op[3]==1): simple, lenWords=1.

## Task 2 — store-data mux (LsEuPlugin S0)
data0 = Mux(u0.stkPush && !u0.srcBValid, u0.imm, rdData.data). Lets a stkPush push a
REGISTER (LINK old An, srcB valid); BSR/JSR (srcB invalid) keep pushing imm (retPC).

## Task 3 — ALU crack-drop marker (AluEuPlugin)
wbObs.divRem := RegNext(Mux(s3Valid, u3.divIsRem, u1.divIsRem)) init False (was False).

## Task 4 — the cracks (MicroOpAssembler)
isLinkOp = op[15:4]==0x4E5 && !op[3]; isUnlkOp = op[15:4]==0x4E5 && op[3].
Add both to the `bad` exemption. linkAn = op[2:0] -> arch 8+reg. disp = sext(words(1)).
- LINK [stkPush store dst=An srcB=An] + [ADD A7:=A7+disp divIsRem] + [ADD An:=A7-disp (kept)].
- UNLK [load (An)->T0] + [ADD A7:=An+4 divIsRem] + [MOVE T0->An (kept)].
Wire into the sequence-selection chain (count=3 each), before the generic crack cases.
The kept µop is uops(2) (last, firstOfInstr=false); uops(0) is firstOfInstr=true.

## Task 5 — decode spec (LinkUnlkDecodeSpec) + lock-step + test-fast
- Decode spec: counts + per-µop fields + predecode lengths (PredecodeRef parity).
- Lock-step (each own JVM, -z): LINK/UNLK round trip (checkMem pushed value), nested,
  +/- disp.
- test-fast regression. CONTROLLER runs the synth gate.
