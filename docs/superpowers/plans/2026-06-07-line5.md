# Line-5 (ADDQ/SUBQ/Scc/DBcc) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development / executing-plans. Checkbox steps, TDD. Gate = LOCK-STEP (correctness) + OOC-synth sanity. NOT post-route (non-deterministic; line-5 is FMax-neutral). STANDING RULE: keep plugin boundaries registered.

**Goal:** ADDQ/SUBQ (#1-8, Dn+An dest), Scc (Dn), DBcc (counted-loop). Lock-stepped vs Musashi.

**Architecture:** ADDQ/SUBQ reuse ADD/SUB (Dn=NZVCX size-merged; An=full-32 no-flags). Scc/DBcc reuse the branch EU's condition eval. DBcc's Dn.W counter is a partial-register read+write (reuse the MOVE.B/.W merge, just merged). DBcc is PC-relative (target=pc+2+disp like Bcc); condition = `!cc && (Dn.W-1 != -1)`.

**Tech Stack:** SpinalHDL / sbt `~/sbt/bin/sbt` / Verilator / Vivado(OOC sanity). Reference: `decode/OperationDecoder.scala`+`MicroOpAssembler.scala` (add line 5), `execute/AluEuPlugin.scala`+`AluDatapath.scala` (ADD/SUB + the .B/.W partial merge from MOVE), `execute/BranchEuPlugin.scala` (16 condition codes, NZVC read — Scc/DBcc), `frontend/PredecodeWord.scala` (DBcc len=2, ADDQ/Scc len=1), `oracle/Musashi.scala`, `lockstep/ExecuteLockStepSpec.scala`. Spec: `docs/superpowers/specs/2026-06-07-line5-design.md`.

**Branch:** `feat/line5` (off master).

---

### Task 1: Diagnose — ADDQ/SUBQ An path, Scc/DBcc condition reuse, DBcc crack, predecode
**Files:** read-only. Confirm: the An (no-flag, full-32) path for ADDQ/SUBQ #n,An (cf. ADDA/SUBA); how to reuse the branch EU's condition eval for Scc + DBcc; the DBcc crack (one branch-EU µop reading Dn + decrementing + cond, vs ALU-decrement + branch); the Dn.W/Dn.B partial-reg merge (reuse MOVE.B/.W); predecode lengths (DBcc=2 words, ADDQ/SUBQ/Scc=1).
- [ ] Capture decisions in the Task-1 commit (empty ok).

### Task 2: ADDQ/SUBQ (Dn + An)
**Files:** `decode/*`, `execute/AluEuPlugin.scala`, Test `decode/Line5DecodeSpec.scala` + lock-step.
Decode `0101 ddd q ss mmmrrr` (q=0 ADDQ/1 SUBQ; ddd 1-8, 0→8; mode 0 Dn / mode 1 An). Dn: ADD/SUB + NZVCX (size-merged). An: full-32 add/sub, no flags.
- [ ] failing test (ADDQ/SUBQ .B/.W/.L Dn flags + An no-flags/full-32) → FAIL → implement → PASS ×2 → commit `line5: ADDQ/SUBQ (Dn flags + An full-32 no-flags)`.

### Task 3: Scc (Dn)
**Files:** `decode/*`, the chosen EU (branch-EU condition path), Test `execute/SccSpec.scala`.
Decode `0101 cccc 11 000rrr` → Dn[7:0] := cond(cccc) ? 0xFF : 0x00 (partial-reg byte write, preserve upper 24, reuse the MOVE.B merge). No flags. 16 conditions incl. ST/SF.
- [ ] failing test (Scc ≥3 conditions, true→0xFF/false→0x00, upper preserved) → FAIL → implement → PASS ×2 → commit `line5: Scc (set byte on condition)`.

### Task 4: DBcc (counted loop)
**Files:** `decode/*`, `execute/BranchEuPlugin.scala`, Test `execute/DbccSpec.scala` + lock-step loop.
Decode `0101 cccc 11001 rrr` + disp16 → if `!cond`: Dn.W -= 1 (partial-reg, preserve upper 16); branch to pc+2+disp if Dn.W != 0xFFFF; if `cond`: fall through. No flags. DBRA/DBF (cc=F) always decrements.
- [ ] failing test (DBRA counted loop runs N times then exits at -1; DBcc with cond true → fall through, false → dec+branch; Dn upper-16 preserved) → FAIL → implement → PASS ×2 → commit `line5: DBcc decrement-and-branch (counted loop)`.

### Task 5: Lock-step + OOC sanity
**Files:** `lockstep/ExecuteLockStepSpec.scala`, run suites.
- [ ] Step 1: lock-step vs Musashi ×2 — ADDQ/SUBQ (Dn+An, .B/.W/.L), Scc (3+ conds), a DBRA counted loop + a DBcc real-condition loop (taken/not/expiry) — value + NZVCX + PC/Dn step-for-step. ALL existing UNCHANGED (ITLB flake → baseline-repro first).
- [ ] Step 2: `make test-fast` + targeted verilator `-z` subsets — report totals.
- [ ] Step 3: OOC sanity (`GenFullCoreSynthVerilog` + `ooc_M68kFullCoreSynth.tcl`): 0 err, no UNASSIGNED REGISTER. No new cross-module combinational crossing (DBcc branch reuse).
- [ ] Step 4: commit `line5: lock-step + OOC sanity`.

---

## Self-Review
**Spec coverage:** ADDQ/SUBQ Dn+An (T2); Scc (T3); DBcc (T4); lock-step+sanity (T5). ✓
**Placeholder scan:** DBcc crack + Scc/An paths are explicit T1 decisions (spec §5). No TBD.
**Type consistency:** ADDQ/SUBQ reuse ADD/SUB + the An path; Scc/DBcc reuse branch-EU conditions; Dn.W/Dn.B partial writes reuse the MOVE.B/.W merge (read old Dn). DBcc PC-relative branch via the existing Bcc path. ✓
