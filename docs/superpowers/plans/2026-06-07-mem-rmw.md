# Memory-destination RMW (load-op-store crack) Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development / executing-plans. Checkbox steps, TDD. Gate = LOCK-STEP + OOC sanity (FMax-neutral; post-route non-deterministic — not gated). STANDING RULE: registered module boundaries.

**Goal:** Memory-destination RMW for ADD/SUB/AND/OR/EOR Dn,<ea>, immediates #imm,<ea>, ADDQ/SUBQ #n,<ea>, CLR/NEG/NEGX/NOT/TST <ea> — MEMSIMPLE EAs ((An)/(d16,An)/abs). Lock-stepped vs Musashi.

**Architecture:** Crack into [load.sz <ea> → T0] + [ALU op (T0 w/ Dn or #imm) → T1 + flags] + [store.sz T1 → <ea>]. MEMSIMPLE EA → load and store recompute the same address (no side effects, no shared-address threading). TST=load+flags (no store); CLR=store-0. Reuses the cracker's memSimple-source mechanism + the LS store/SQ + the existing ALU ops.

**Tech Stack:** SpinalHDL / sbt `~/sbt/bin/sbt` / Verilator / Vivado(OOC sanity). Reference: `decode/MicroOpAssembler.scala` (the memSimple-source load crack + AssembledUops 2→3 from RTR/DIV64 — the template; add the trailing store for mem-dest), `decode/OperationDecoder.scala` (the `isRmw` opmode 4/5/6 path, currently illegal; line-0/4/5 mem-dest), `execute/LsEuPlugin.scala`+`ls/StoreQueue.scala` (store + SQ-forward + the just-merged BRAM/store-pipeline — DON'T regress), `decode/EaDecoder.scala` (MEMSIMPLE), `oracle/Musashi.scala`, `lockstep/ExecuteLockStepSpec.scala`. Spec: `docs/superpowers/specs/2026-06-07-mem-rmw-design.md`.

**Branch:** `feat/mem-rmw` (off master).

## CRITICAL memory: heavy jobs ONE AT A TIME; `-z` subsets for lock-step (12G OOM otherwise).

---

### Task 1: Diagnose the crack + same-address load/store + EA recompute
**Files:** read-only `MicroOpAssembler.scala`, `LsEuPlugin.scala`, `StoreQueue.scala`. Confirm: how the memSimple-SOURCE load crack works (extend it with a trailing store for mem-DEST); the 3-µop AssembledUops support; how the store µop recomputes the same MEMSIMPLE EA (thread base/disp); that a store immediately after its producing load (same address, T0→T1→store dep) is handled by the LS pipe + SQ-forward (no self-hazard — load reads before store allocs).
- [ ] Capture decisions in the Task-1 commit (empty ok).

### Task 2: The RMW crack — ADD/SUB/AND/OR/EOR Dn,<ea> + CLR/NEG/NEGX/NOT/TST <ea>
**Files:** `decode/OperationDecoder.scala`+`MicroOpAssembler.scala`, Test `decode/MemRmwDecodeSpec.scala` + lock-step.
Crack the `isRmw` (opmode 4/5/6) ALU forms + line-4 unary mem-dest into [load→T0][op→T1+flags][store T1→ea]. TST=load+flags (no store); CLR=store-0 (no load needed, or dummy). MEMSIMPLE only (non-MEMSIMPLE → illegal).
- [ ] failing test (ADD/AND Dn,(An) RMW: memory updated + flags; NEG/NOT (An); TST (An) no-store; CLR (An)) → FAIL → implement → PASS ×2 → commit `mem-rmw: load-op-store crack (ALU Dn,<ea> + CLR/NEG/NOT/TST)`.

### Task 3: Immediate + quick mem-dest forms
**Files:** `decode/*`, Test extend.
ADDI/SUBI/ANDI/ORI/EORI #imm,<ea> (line 0 mem-dest); ADDQ/SUBQ #n,<ea> (line 5 mem-dest). Same crack with #imm/#n as the op's 2nd operand. CMPI #imm,<ea> = load+compare, NO store.
- [ ] failing test (ADDI/ANDI/EORI #x,(An); ADDQ #n,(An); CMPI #x,(An) no-store) → FAIL → implement → PASS ×2 → commit `mem-rmw: immediates + ADDQ/SUBQ to memory`.

### Task 4: Lock-step + OOC sanity
**Files:** `lockstep/ExecuteLockStepSpec.scala`, run suites.
- [ ] Step 1: lock-step vs Musashi ×2 — the RMW ops to (An)/(d16,An)/abs (.B/.W/.L, flag+carry/overflow edges), TST-mem/CLR-mem/CMPI-mem no-write — final MEMORY value + NZVCX step-for-step. ALL existing UNCHANGED (esp. store/load/SQ-forward/loop-store — don't regress the LS work; ITLB flake → baseline-repro first).
- [ ] Step 2: `make test-fast` + targeted verilator `-z` subsets — report totals.
- [ ] Step 3: OOC sanity (`GenFullCoreSynthVerilog` + `ooc_M68kFullCoreSynth.tcl`): 0 err, no UNASSIGNED REGISTER. No new cross-module crossing.
- [ ] Step 4: commit `mem-rmw: lock-step + OOC sanity`.

---

## Self-Review
**Spec coverage:** RMW crack ALU+unary (T2); immediates+ADDQ/SUBQ mem (T3); lock-step+sanity (T4). ✓
**Placeholder scan:** the crack mechanism + EA-recompute + same-address handling are explicit T1 decisions (spec §5). No TBD.
**Type consistency:** crack = [load→T0][op→T1+flags][store T1→ea]; store recomputes the MEMSIMPLE EA; reuses ALU ops + the LS store/SQ; TST/CMPI no-store, CLR store-0. 3-µop AssembledUops (already 2→3). ✓
