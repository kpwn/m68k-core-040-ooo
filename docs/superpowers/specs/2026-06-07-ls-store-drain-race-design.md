# LS store-drain race: dropped store under tight store->load(same)->store(same)

BUG (pre-existing, found by mem-RMW, repro on master): a SQ/dcache store-drain race DROPS A STORE under sustained tight `store->load(same-addr)->store(same-addr)` across lines. mem-RMW (load-op-store same addr) hits it; repro deterministically with a NON-RMW 3x store-load-store-same-addr program (across cache lines). Root-cause in `ls/StoreQueue.scala` (drain head/tail, resident-until-AXI-B-ACK window, the SQ-forward) + `cache/DcachePlugin.scala` (write-through aw/w/B handshake, the store-S0/S1 pipeline, single-outstanding). Likely: a 2nd store to the same address allocates/drains while the 1st store is still draining (B not yet acked) -> the resident-window/forward/drain interaction loses one. FIX the root cause (no band-aid). VALIDATE: the minimal repro passes + a multi-RMW loop (e.g. add #1,(An) x N) gives the correct final memory vs Musashi + ALL existing LS lock-step UNCHANGED + honest gate.
