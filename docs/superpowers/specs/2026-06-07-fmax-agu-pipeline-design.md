# FMax recovery: pipeline the LS-EU misalign AGU cone (s1Src2->s1AddrB)

Current OOC FMax 156.7 MHz; the dominant limiter (WNS -2.383, far worse than anything else) is `AluEuPlugin.s1Src2 -> LsEuPlugin.s1AddrB` — 25 logic levels, 4x CARRY8 (chained adders), 72% route, crossing the ALU->LS boundary. ROOT: `addrB0 = (va0 & ~15) + 16` (LsEuPlugin :204) is computed from the COMBINATIONAL `va0` (the AGU effective address = base+disp), and the operand feeding va0 comes across the ALU->LS boundary -> a ~4-adder combinational chain in ONE cycle.

FIX (latency-agnostic — misaligned/cross-line slot-B accesses are ALREADY a later cycle; lock-step is latency-agnostic): compute `s1AddrB` (and the `crossLine`/`crossPage` flags if they share the deep cone) from the REGISTERED `s1Va` the NEXT cycle, NOT the combinational `va0`. The AGU cone then ENDS at `s1Va` (register); addrB is a separate `(s1Va & ~15)+16` from the flop. Also REGISTER the cross-module operand at the LS boundary if `s1Src2` feeds the AGU combinationally from the ALU (standing rule: registered module boundaries). DIAGNOSE the exact 25-level chain first (what are the 4 adders + why s1Src2 crosses from AluEu).

GATE: lock-step ALL UNCHANGED x2 (esp. misaligned/cross-line/cross-page store-load — slot B uses addrB; the +1 cycle is invisible). Deterministic: the `s1Src2->s1AddrB` cone GONE from the worst-path list; OOC FMax materially up from 156 (the next cone is far shallower). Standing rule: no new cross-module combinational crossing. Reference: `execute/LsEuPlugin.scala` :198-260 (addrB0/va0/s1Va/crossLine/crossPage/s1AddrB), the misalign two-access slot-B path.
