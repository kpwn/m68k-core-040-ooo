agent=claude-physical-placement-lever
task=Ground the two nets handoff 25.4 named as the concrete place a floorplan
     attempt should aim; find or refute a targeted placement / MAX_FANOUT /
     relative-placement intervention.
reserved_at=2026-08-11T12:35:00+02:00
released_at=2026-08-11T14:20:00+02:00
head=RTL untouched, byte-identical to c776f06 (netlist body md5 266a1874...)

RESULT = THE TARGETED-PLACEMENT LEVER IS REFUTED, three ways, plus two more
  levers closed underneath it.
  1. NAMING.  The two nets are ONE net and it is not in the ibuf.
     `when_PipeStage_l17` is PipeStage.scala:17 `slotFree` for the DecodeStage
     `raw` stage (= rawIn.ready, section 17's reverse-READY chain, already priced
     at +0.006 ns).  The `FetchAlignPlugin_logic_ibuf/` prefix is section 18 step
     3's instance-path trap.  `p_1542_in` has ZERO matches in the generated
     Verilog and shares the identical worst path.
  2. SLACK.  Worst path through either is -1.451 vs the -1.472 WNS -- 0.021 ns
     BEHIND.  False-pathing them and all 7 PipeStage slotFree enables is
     +0.000 ns.  Baseline reproduced exactly; confirmed from the endpoint side.
  3. GEOMETRY.  p_1542_in's driver is at SLICE_X47Y38; its 875 leaf loads have
     centroid X47.3 Y34.5 -- ALREADY on its own load centroid.  Max Manhattan
     28/60 CLB.  No placement error exists for a pblock/MAX_FANOUT/LOC to fix.
  ROOT CAUSE OF THE MIS-DISPATCH: 25.4's path was the residual of the a3_cluster
     WHAT-IF, inherited as a fact.  Second occurrence (24.5 was the first).
  BONUS CLOSED: the design's only BUFG (placer-inserted, fo 1057, pre-BUFG span
     101 CLB) -- correct call, that net has 0.657 ns of margin.
  SYNTHESIS AXIS CLOSED: PerformanceOptimized -36.27 MHz, AlternateRoutability
     -15.64 MHz.  All four tool axes now closed.
  SPEED GRADE CLOSED: xcku5p-ffvb676-3-e measures -1.620 / 177.936 MHz, i.e.
     4.81 MHz SLOWER than the -2 part.  Raw silicon advantage on this
     route-dominated netlist is only ~0.100 ns (1.8%), not the 10-15% rule of
     thumb -- it caps the lever at ~21% of the gap even before the stall.
  THE GENERAL FINDING (six-for-six): every intervention that hands the iterated
     post-route loop an EASIER problem -- faster part, stronger directive, better
     initial placement -- converges to a WORSE final number.  The +18.65 MHz is a
     property of the ITERATION, not of effort.
  POPULATION: 4890 endpoints / 29 startpoint families below -1.000; only 4
     families in the top 0.022 ns.  200 MHz is a distribution problem.
NOTHING IMPLEMENTED -- the measurement said there was nothing to implement.
BRANCH STATE=CLEAN, still -1.472 / 182.749 MHz.  test-fast 149/149 (157 suites);
  testOnly 460/460 with 7 requested / 7 completed.
MACHINE RULE LEARNED: run fresh syntheses ONE at a time (synth_design spawns ~9
  workers; two already thrash).  `-retiming` was launched and not completed.
WRITEUP=handoff section 26 (26.1-26.9); probes synth/probe_net_geometry{,2}.tcl,
  synth/probe_net_lever.tcl, synth/synth_directive_ab.sh; evidence
  synth/probe_netgeo{,2}/, synth/probe_netlever/, synth/probe_synthdir{,2}/,
  synth/probe_part3/.
