agent=claude-physical-placement-lever
task=Ground the two nets handoff 25.4 named as the concrete place a floorplan
     attempt should aim, at the PHYSICAL level; find or refute a targeted
     placement / MAX_FANOUT / relative-placement intervention.
reserved_at=2026-08-11T12:35:00+02:00
status=ACTIVE
head=acbc619 (docs+reservation only) / RTL untouched

INTERIM RESULT = the named target is REFUTED, on three independent grounds.
  1. NAMING.  `ibuf/when_PipeStage_l17` and `queue/p_1542_in` are not two nets and
     neither is in the instruction buffer.  `when_PipeStage_l17` is PipeStage.scala:17
     `slotFree` for the DecodeStage `raw` stage (= rawIn.ready, section 17's reverse
     READY chain); the `FetchAlignPlugin_logic_ibuf/` prefix is the section-18-step-3
     instance-path artefact.  The same net also resolves as
     `DecodeStage_logic_queue/when_PipeStage_l17`.  `p_1542_in` is a synth-created
     name in the same cone (0 matches in the generated Verilog).
  2. SLACK.  Worst path THROUGH either is -1.451 -- 0.021 ns BEHIND the -1.472 WNS.
     False-pathing them, and all 7 PipeStage slotFree enables in the design, is
     +0.000 ns.  Baseline reproduced exactly first.
  3. GEOMETRY.  Max driver-to-load Manhattan is 28 and 48 CLB -- the same
     "quarter of the die, not a die-length haul" section 18 step 2 already
     classified as a LOAD problem, not a distance problem.  The dispatch's
     "placed far apart" hypothesis is refuted by coordinates.
  BONUS: the placer inserted the design's ONLY BUFG on `when_PipeStage_l17_2`
     (fanout 1057, pre-BUFG span 101 CLB).  Grounded and cleared -- that net's
     worst path is -0.815, 0.657 ns of margin.  Not a lever either.
  POPULATION: 4890 endpoints below -1.000 (the 200 MHz bar); 98 below -1.400;
     16 below -1.450.  200 MHz is a ~5k-endpoint distribution problem.
IN FLIGHT = the campaign's one named untried PHYSICAL lever (section 18 step 6
     lever 4 / section 19 step 8): a fresh synth_design under non-default recipes
     (PerformanceOptimized, AlternateRoutability, -retiming) with the landed
     decode+postrouteN3 flow.  synth/synth_directive_ab.sh, synth/probe_synthdir/.
NOTE 2026-08-11T13:5x: three concurrent FULL syntheses exhausted RAM (40.6 GB RSS,
  20 GB swap) -- synth_design spawns ~9 worker processes per run, unlike the
  impl-only REUSE_SYNTH_DCP runs section 18 sized at "three at a time".  The
  `retime` variant was killed to recover; PerformanceOptimized and
  AlternateRoutability continue.  BUDGET RULE FOR FUTURE DISPATCHES: at most TWO
  concurrent fresh-synthesis runs on this machine.
