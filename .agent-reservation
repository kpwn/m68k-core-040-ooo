agent=claude-elab-lottery
task=Cheap experiment: does a fresh GenFullCoreSynthVerilog re-elaboration land a DIFFERENT
     netlist than the frozen checkpoint every campaign number descends from?
reserved_at=2026-08-11T00:10:00+02:00
released_at=2026-08-11T00:35:00+02:00
head=a9f14ff. NO RTL CHANGED. NO SYNTH RUN.

RESULT=THE LOTTERY TICKET IS VOID. ELABORATION IS DETERMINISTIC ON THIS BRANCH.
  `3e57fe6 fix(regfile): pin physical write-port slot order` IS an ancestor of HEAD
  (LinkedHashMap first-appearance grouping, not `groupBy` over identity-hash keys).
  Swept for other instances of the hazard: NONE (`groupBy` survives in src/ only
  inside that file's own do-not-reintroduce comment).
  GROUNDED, not assumed -- two cold-JVM re-elaborations:
    archived baseline  d80f6218c5c7dcab94a33a52d64244fa
    fresh roll #1      d79df4c8d7ba87cb9f499ca878fb6dbd
    fresh roll #2      d79df4c8d7ba87cb9f499ca878fb6dbd  (identical to #1)
    comment-stripped, ALL THREE = 3531a01b346df92e2e14c389ef47289b
  Entire diff vs baseline = ONE COMMENT LINE (`// Git hash`), which Vivado ignores.
  Re-synthesising would burn a post-route gate to reproduce -1.472ns/182.749MHz exactly.

BYPRODUCT=the archived netlist's `// Git hash` header (9a013ae) is STALE -- it predates the
  section-14 FTB retime -- but the NETLIST IS NOT: the retime's signals are present in the
  archived baseline and the fresh rolls in identical counts (qFramed x3, qEnd x7). Use the
  COMMENT-STRIPPED hash 3531a01b346df92e2e14c389ef47289b as the durable netlist identity;
  the full-file md5 changes every commit via that header and is worthless as a change check.
  generated/M68kFullCoreSynth.v restored bit-for-bit to d80f6218 so sections 13-20 resolve.

VALUE=removes an outstanding doubt about EVERY A/B in sections 13-20 -- those gates compared
  netlists differing only by their intended RTL edit, with no lottery noise underneath.
  Section 20's conclusion is unchanged and has NO cheap alternative left in front of it.
gate=none needed (no RTL changed, baseline file restored byte-for-byte)
